PREMIUM_API_BASE_URL=
AI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
AI_MODEL=gemini-flash-lite-latest
# The following are NOT set here — they're real secrets, stored in Google
# Secret Manager instead (never in a plain .env file):
#   firebase functions:secrets:set PREMIUM_WEBHOOK_SECRET
#   firebase functions:secrets:set PREMIUM_API_KEY
#   firebase functions:secrets:set AI_API_KEY
